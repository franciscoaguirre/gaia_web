= render 'shared/top_background.html.haml', title: t('store')

.full-height-wrapper.d-flex.flex-column.justify-content-center
  .products.row.no-gutters.justify-content-around
    - @products.each do |product|
      = render product

= render 'shared/bottom_background.html.haml'
